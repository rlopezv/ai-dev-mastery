Write all documents for a module in sequence.

Steps:
1. Read `meta/system-design/DOCS_LABS_MAP.md` for the module scope and topic list
2. Write documents in this order: README → topics → architecture → implementation-reference → validation
3. For each document:
   a. Read the appropriate template
   b. Write the full document
   c. Validate before proceeding to the next
4. Update `docs/reference/glossary.md` with all new concepts
5. Update `meta/session/PROJECT_STATUS.md`
